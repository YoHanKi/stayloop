apiVersion: 1
providers:
  - name: 'stayloop-local'
    orgId: 1
    folder: 'Stayloop'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
